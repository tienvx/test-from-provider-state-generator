{
  "name": "test-from-provider-state-generator",
  "version": "1.0.0",
  "license": "MIT",
  "dependencies": {
    "@pact-foundation/pact": "^12.1.0",
    "axios": "^1.4.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6",
    "vitest": "^0.34.1",
    "wait-port": "^1.0.4"
  },
  "scripts": {
    "test": "rm -f ./pacts/*.json && vitest run --no-threads --silent --reporter dot"
  }
}
